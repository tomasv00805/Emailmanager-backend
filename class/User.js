// creame una clase usuario que los atributos sean username, email y password

class User {
  constructor (username, email, password) {
    this.username = username
    this.email = email
    this.password = password
  }
}
module.exports = User
